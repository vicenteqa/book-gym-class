name: Book CIRCUIT TRAINING for Thursday 18:00
on:
    workflow_dispatch:
    schedule:
        - cron: '45 15 * * 2' # Runs every Tuesday at 17:45h // 2h difference with server.

env:
    GYMUSERNAME: ${{ secrets.GYMUSERNAME }}
    PASSWORD: ${{ secrets.PASSWORD }}
jobs:
    test:
        timeout-minutes: 60
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version: 18
            - name: Install dependencies
              run: npm ci
            - name: Install Playwright Browsers
              run: npx playwright install --with-deps
            - name: Run Playwright tests
              run: ACTIVITY='CIRCUIT TRAINING' ACTIVITY_TIME='18:00' npx playwright test
            - uses: actions/upload-artifact@v4
              if: ${{ !cancelled() }}
              with:
                  name: playwright-report
                  path: playwright-report/
                  retention-days: 1
