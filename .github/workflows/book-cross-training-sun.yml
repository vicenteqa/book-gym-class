name: Book CROSS TRAINING for Tuesday 17:45
on:
    workflow_dispatch:
    schedule:
        - cron: '30 15 * * 0' # Runs every Sunday at 17:30h // 2h difference with server.

env:
    GYMUSERNAME: ${{ secrets.GYMUSERNAME }}
    PASSWORD: ${{ secrets.PASSWORD }}
jobs:
    test:
        timeout-minutes: 60
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version: 18
            - name: Install dependencies
              run: npm ci
            - name: Install Playwright Browsers
              run: npx playwright install --with-deps
            - name: Run Playwright tests
              run: ACTIVITY='CROSS TRAINING' ACTIVITY_TIME='17:45' npx playwright test
            - uses: actions/upload-artifact@v4
              if: ${{ !cancelled() }}
              with:
                  name: playwright-report
                  path: playwright-report/
                  retention-days: 30
